dependencies {
    compile libs.'spring-jdbc' + '@jar',
            libs.'spring-tx' + '@jar',
            libs.'jsqlparser',
            project(':lit-commons')
    compileOnly libs.'spring-context'

}
