dependencies {
    compile libs.'spring-jdbc' + '@jar',
            libs.'spring-tx' + '@jar',
            libs.'spring-aspects',
            project(':lit-commons')
    compileOnly libs.'spring-context'

}
